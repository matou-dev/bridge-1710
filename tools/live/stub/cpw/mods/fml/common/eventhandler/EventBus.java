package cpw.mods.fml.common.eventhandler;

/** B3 compile stub, never runs (see Mod.java). {@code register} serves
 * forge/ (pinned by tools/run-live.sh); {@code post} serves the dev-only
 * autoplay spike proof (pinned by tools/autoplay/universal-pin.txt). */
public class EventBus {
    public void register(Object target) {
    }

    public boolean post(Event event) {
        return false;
    }
}
