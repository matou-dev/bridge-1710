package net.minecraft.world;

/**
 * B3 compile stub: shape-only 1.7.10 ChunkCache reference for the
 * RenderWorldEvent.Pre stub signature. Never runs (compile classpath
 * only) — only the type name rides the reobf map (MCP to notch), no
 * member is ever read.
 */
public class ChunkCache {
}
