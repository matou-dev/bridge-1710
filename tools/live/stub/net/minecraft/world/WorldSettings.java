package net.minecraft.world;

/**
 * B3 compile stub: descriptor-only 1.7.10 WorldSettings (the companion
 * passes null, the type only names the launchIntegratedServer slot).
 * Merged here from tools/autoplay/stub (single-stub discipline — a
 * second Minecraft-adjacent tree is a duplicate-class error waiting to
 * happen). Class names are identical in searge and MCP, so no mapping is
 * ever needed — same practice as bridge-1122.
 */
public class WorldSettings {
}
