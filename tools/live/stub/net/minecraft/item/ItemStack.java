package net.minecraft.item;

/**
 * B3 compile stub: shape-only vanilla stack used by the loot sink in
 * forge/ (MatouBridgeMod wraps one carrier per drop, pinned to the
 * 1.7.10 SRG by tools/run-live.sh). Never runs (compile classpath only).
 */
public class ItemStack {
    public ItemStack(Item item, int count) {
    }

    public Item getItem() {
        return null;
    }
}
