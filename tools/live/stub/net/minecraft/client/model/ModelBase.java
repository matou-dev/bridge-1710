package net.minecraft.client.model;

/**
 * B3 compile stub: shape-only vanilla model base used by {@code forge/}
 * sources. Never runs (compile classpath only). Serves the beast renderer
 * mapping in forge/ (hub decisions/SPAWN.md, custom entity tranche).
 * Drift fails loudly on the owning side. Client-only: only ever
 * referenced from {@code @SideOnly(CLIENT)} code.
 */
public class ModelBase {
}
