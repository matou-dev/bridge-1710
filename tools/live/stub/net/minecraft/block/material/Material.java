package net.minecraft.block.material;

/**
 * B3 compile stub: shape-only vanilla API used by {@code forge/} sources.
 * Never runs (compile classpath only). {@code rock} serves
 * {@code MatouBlock} (pinned to the 1.7.10 SRG by tools/run-live.sh) —
 * drift fails loudly.
 */
public class Material {
    public static Material rock;
}
