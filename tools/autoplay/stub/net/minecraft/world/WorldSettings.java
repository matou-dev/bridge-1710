package net.minecraft.world;

/**
 * Autoplay compile stub: descriptor-only 1.7.10 WorldSettings (the
 * companion passes null, the type only names the launchIntegratedServer
 * slot). Class names are identical in searge and MCP, so no mapping is ever
 * needed — same practice as bridge-1122.
 */
public class WorldSettings {
}
