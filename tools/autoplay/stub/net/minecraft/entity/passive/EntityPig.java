package net.minecraft.entity.passive;

import net.minecraft.entity.EntityLivingBase;
import net.minecraft.world.World;

/**
 * Autoplay compile stub: shape-only vanilla pig used by the dev-only
 * companion loot proof (tools/autoplay/, never shipped), which spawns one
 * as the kill author for a simulated {@code LivingDropsEvent}. Class names
 * are identical in searge and MCP, so no mapping is ever needed — same
 * practice as bridge-1122. Never runs (compile classpath only).
 */
public class EntityPig extends EntityLivingBase {
    public EntityPig(World world) {
    }
}
